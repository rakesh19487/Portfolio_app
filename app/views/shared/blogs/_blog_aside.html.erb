<aside class="col-md-4 blog-sidebar">
  <div class="p-3 mb-3 bg-secondary rounded">
    <h4 class="font-italic">About</h4>
    <p class="mb-0">Etiam porta <em>sem malesuada magna</em> mollis euismod. Cras mattis consectetur purus sit amet fermentum. Aenean lacinia bibendum nulla sed consectetur.</p>
  </div>
  <% if !session_source.nil? %>
    <div class="p-3 mb-3 rounded alert alert-info">
      <%= session_source %>
    </div>
  <% end %>
  <div class="p-3 mb-3 bg-light rounded">
    <h4>Topics</h4>
    <% @side_bar_topics.each do |topic| %>
      <p><%= link_to topic.name.capitalize, topic_path(topic) %></p>
    <% end %>
  </div>
  <div class="p-3">
    <h4 class="font-italic">Elsewhere</h4>
    <ol class="list-unstyled">
      <li><%= fa_icon("github")%></li>
      <li><%= fa_icon("twitter")%></li>
      <li><%= fa_icon("facebook")%></li>
    </ol>
  </div>
</aside>
<!-- /.blog-sidebar -->
